// Quick Start Guide - Pizza Pizzeria App

// 1. VIEW THE APP IN BROWSER
npm run dev
// Visit: http://localhost:3000/

// 2. WHAT YOU'LL SEE
- 🍕 Pezza Pizzeria header with tagline
- Grid of delicious pizza cards
- Each card shows: image, name, description, price, toppings, badges
- "Add to Cart" button on each pizza

// 3. ADD PIZZAS TO CART
- Click "Add to Cart" button
- Watch the cart count increase (🛒 Cart (1), 🛒 Cart (2), etc.)

// 4. OPEN SHOPPING CART
- Click "🛒 Cart (N)" button in header
- Sidebar slides in from right
- Shows all items in cart

// 5. MANAGE CART ITEMS
- Use + / - buttons to adjust quantity
- Click 🗑 to remove item
- See subtotal update in real-time

// 6. PLACE AN ORDER
- Fill customer info in the sidebar:
  * Full Name (required)
  * Email (required)
  * Phone (required)
  * Address, City, Zip (optional)
- Click "Place Order" button
- Order sent to https://localhost:7160/api/orders

// 7. PROJECT STRUCTURE
src/features/pizzas/
├── types/index.ts          - Pizza, Order, Cart types
├── hooks/usePizzas.ts      - React Query hooks
└── pages/
    ├── PizzaMenuPage.tsx   - Main UI component
    └── PizzaMenuPage.scss  - Pezza theme styling

// 8. THEME COLORS
Primary Red:     #dc3348  - Buttons, accents, headers
Secondary Gold:  #fec844  - Highlights, secondary buttons

// 9. BUILD COMMANDS
npm run dev           - Start dev server
npm run build         - Production build
npm run type-check    - Check TypeScript
npm run preview       - Preview production build

// 10. API CONFIGURATION
API Base URL: https://localhost:7160
All requests go to this .NET backend

// 11. FEATURES INCLUDED
✅ Pizza menu grid
✅ Responsive design (mobile-first)
✅ Shopping cart with local state
✅ Add/remove/quantity controls
✅ Customer info form
✅ Order creation
✅ Dark mode support
✅ Smooth animations
✅ Error handling
✅ Loading states
✅ Toast notifications

// 12. TESTED & VERIFIED
✅ TypeScript compilation: PASS
✅ Production build: 4.57s, 70.78 kB gzip
✅ No errors or warnings
✅ Ready for deployment

🚀 Application is ready to run!
