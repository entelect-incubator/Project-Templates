# Environment Configuration Example
# Copy this file to .env for local development

# API Configuration
# The base URL for the Pizza Tracker API
# 
# IMPORTANT: Backend Setup
# 1. Standalone API: https://localhost:5001 (default)
#    - Run: cd backend/.NET/{ArchitectureTemplate}/Api && dotnet run
#
# 2. With Aspire (OpenTelemetry Dashboard):
#    - Run: cd backend/.NET/{ArchitectureTemplate}/AspireHost && dotnet run
#    - Aspire Dashboard: http://localhost:15258
#    - API will be available at http://localhost:5000
#    - Update VITE_API_BASE_URL=http://localhost:5000
#
# 3. Production: Replace with your actual domain
VITE_API_BASE_URL=https://localhost:5001

# Alternative API URL variable (for server-side compatibility)
API_BASE_URL=https://localhost:5001

# Node Environment
# Values: development, production, test
NODE_ENV=development

# Logging Configuration
# Log level for development
# Values: error, warn, info, debug
LOG_LEVEL=info

# Feature Flags
VITE_ENABLE_DEVTOOLS=true

# External Logging Service (Optional)
# Uncomment and configure for production
# SENTRY_DSN=your-sentry-dsn
# DATADOG_API_KEY=your-datadog-key
# LOGROCKET_APP_ID=your-logrocket-id

# React Query Configuration
# Query stale time in milliseconds (default: 300000 = 5 minutes)
VITE_QUERY_STALE_TIME=300000

# Error Boundary Configuration
# Show detailed errors in development
VITE_SHOW_ERROR_DETAILS=true
